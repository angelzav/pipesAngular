import { OcultatextoPipe } from './ocultatexto.pipe';

describe('OcultatextoPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultatextoPipe();
    expect(pipe).toBeTruthy();
  });
});
