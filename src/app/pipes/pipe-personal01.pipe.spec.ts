import { PipePersonal01Pipe } from './pipe-personal01.pipe';

describe('PipePersonal01Pipe', () => {
  it('create an instance', () => {
    const pipe = new PipePersonal01Pipe();
    expect(pipe).toBeTruthy();
  });
});
